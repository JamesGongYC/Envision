# v3.1 — Day 2 Ticket: Evolution + How-it-works + Disclaimer + Ship

**Sprint:** v3.1 frontend refactor (2-day split)
**Scope of this ticket:** the three secondary pages and the polish/deploy pass. **Tackle Task 1 (Evolution tree) first — it's the single biggest build of the sprint.**
**Read first:** `docs/v3.1_frontend_plan.md`, `PROGRESS_v3_addendum.md` §3 (backtest-Brier caveat), `envision_plan.md` §12 (disclaimer) + §5 (attribution). Backend/agent/DB still **untouched** — `viewer/` only. Day 1 foundation (dark tokens, routes, nav/footer, redirects, removed StatusHeader) is assumed in place.

---

## Pre-flight
1. `git status` clean; Day-1 work merged. Routes `/evolution`, `/disclaimer`, `/how-it-works` exist as placeholders.
2. Install graph deps: `npm i @xyflow/react @dagrejs/dagre`.
3. Confirm `skill_lineage`, `backtest_run`, `forecasts_shadow`, `shadow_evaluations` tables + `skill_edit_proposals.lineage_id` exist (migration 006). Confirm the old status-query fns survived Day 1 (reused in Task 2).

## NEVER-CUT
- **Disclaimer banner stays global** (Day 1). Locked §12 text in full on `/disclaimer`.
- **Attribution not deleted** — sources/methodology relocate to How-it-works (Task 2), they don't vanish.

---

## Tasks

### 1 — Evolution page (`/evolution`) — replaces old `/agent` *(do first; largest)*
**Layout:** top ~20% = a one-line explainer of the page **plus** the Brier / FP / Hit definitions block, moved up-front. Bottom ~80% = the lineage tree expanding downward.

- **Remove this paragraph entirely** (it's on the current page):
  > "Envision is an experimental, self-evolving agent system that monitors global wildfires and tropical cyclones. Detection skills consume signals from public data sources and emit probabilistic forecasts; an evaluator scores forecasts against ground truth events; a curator periodically proposes refinements to the skill library, gated by operator review. The system is research, not a calibrated alerting product."
- **Metric legend** (`components/metric-legend.tsx`): the Brier/FP/Hit definitions, previously hover-tooltips (`lib/tooltips.ts`), now a prominent block near the top.
- **Lineage data** (`lib/lineage-queries.ts`, server-side, read-only):
  - Nodes from `skill_lineage` (skill_id, parent_skill_id, version, generation_method ∈ manual|mutated|generated, status ∈ candidate|shadow|promoted|archived). Edges from `parent_skill_id`.
  - Per-node stats (Brier 4dp, hits, FP, misses, emitted) aggregated **from live `evaluations`** per `(skill_id, version)`.
  - **Show LIVE Brier, not backtest Brier** (addendum §3: backtest gate unvalidated). Shadow candidates render as a distinct state ("evaluating · N/20"), never ranked by backtest.
  - Handle `lineage_id IS NULL` legacy/orphan proposals gracefully — don't crash the tree.
- **Tree** (`components/skill-tree.tsx`, `skill-node.tsx`, `lineage-edge.tsx`) — React Flow client island, data fetched server-side and serialized in:
  - dagre top→bottom layout; one chain per skill family, branches for shadow candidates.
  - **Unclicked node** = one-line, user-friendly description. **Click** → expand/dropdown with Brier/FP/hits/version/generation_method + sparkline (reuse `brier-sparkline.tsx`).
  - **Edges = rolling dotted lines:** custom React Flow edge, SVG `stroke-dasharray` + `@keyframes` animating `stroke-dashoffset`. **Gate behind `prefers-reduced-motion`** (static dashes when reduced).
  - **Empty/sparse state:** cold-start lineage may be near-linear with zero promotions (addendum §9) — must render cleanly, not error.
  - Lazy-load the island; it's the only React Flow page.
- **Accept:** intro paragraph gone; metric legend at top; tree renders top→down with animated (reduced-motion-safe) dotted edges; nodes show one-liner collapsed, full stats expanded; live Brier shown, shadow nodes distinct; sparse data renders without error.

### 2 — How-it-works page (`/how-it-works`) — the new "About"
- `components/how-it-works/pipeline-figure.tsx` + `pipeline-panel.tsx`: three panels **Ingest → Forecast → Evolve**. Collapsed, they connect left-to-right as one coherent pipeline (visible connectors). Click any panel → expand to detail in the format *"The [name] layer is responsible for …; every [cadence], it …."*
  - **Ingest:** pulls from public sources (FIRMS / NWS / NHC / JTWC / Open-Meteo / ECMWF / AIFS, + GDACS ground truth) on per-source cadences (30 min → 12 h); normalizes to `signals`. **Relocate sources/methodology/attribution here** (from old `/about`).
  - **Forecast:** detection skills read signals each cadence and emit probabilistic forecasts (cap 0.85) with LLM reasoning.
  - **Evolve:** every 24 h the curator reads Brier, mutates skill code, backtests + shadow-evaluates, queues operator-gated promotions.
- **Relocate telemetry** (reuse Day-1-preserved status-query fns): `last ingestion` (= `max(timestamp)` from signals) → **Ingest** panel; `skills active` → **Forecast** panel; `curator status / last proposal` (= `max(proposed_at)`) → **Evolve** panel.
- Expand animation respects `prefers-reduced-motion`.
- **Accept:** three connected panels read as a pipeline collapsed; each expands to a coherent detail; the three telemetry facts live in their matching panels; attribution present here.

### 3 — Disclaimer page (`/disclaimer`) — trimmed old `/about`
- Trim to the locked **§12 text in full** + the §13 non-goals. Anything else (sources/methods) already moved to How-it-works (Task 2) — don't duplicate, don't delete.
  > Locked §12: "Envision is an experimental research artifact built to explore self-evolving agent architectures for disaster signal detection. It is not an alerting service and must not be used for safety-critical decisions. For authoritative information consult the U.S. National Weather Service (weather.gov), the National Hurricane Center (nhc.noaa.gov), the Japan Meteorological Agency (jma.go.jp), or your local emergency management authority. Forecasts published here are produced by an automated system with limited validation and known false-positive rates."
- Footer-only entry (set Day 1). Keep `<meta name="description">` truncated disclaimer.
- **Accept:** §12 text verbatim + non-goals present; no orphaned/duplicated sources block; reachable from footer.

### 4 — Less-wordy copy pass + QA + deploy
- Copy pass across all pages: trim research-hedging prose to commercial brevity (without removing NEVER-CUT items).
- QA: dark contrast/legibility sweep; arrow + expand + edge animations under `prefers-reduced-motion`; broken-link sweep (incl. both redirects); sparse-lineage empty state; React Flow island lazy-loads and doesn't bloat first paint.
- Deploy: Vercel Root Dir `viewer/`; confirm `DATABASE_URL` in all three Vercel envs (v1 gotcha); deploy from `main`.
- **Accept:** clean build; all four pages render dark and commercial; redirects + links resolve; deployed.

---

## Day-2 done = all of:
- [ ] Evolution: intro removed; metric legend up top; downward React Flow lineage tree with reduced-motion-safe rolling dotted edges; collapsed one-liners / expanded stats; **live** Brier; shadow nodes distinct; sparse state safe.
- [ ] How-it-works: 3 connected panels (Ingest→Forecast→Evolve), expand-on-click, telemetry distributed into the right panels, attribution relocated here.
- [ ] Disclaimer: §12 verbatim + non-goals; footer-only; meta description set.
- [ ] Copy trimmed; QA passed; deployed from `main`.
- [ ] `PROGRESS.md` v3.1 closeout note added (record the `/about`→`/how-it-works` rename + Detections/Atmosphere layer names so future readers aren't confused).

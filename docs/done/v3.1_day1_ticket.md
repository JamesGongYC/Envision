# v3.1 — Day 1 Ticket: Foundation + Landing + Forecasts

**Sprint:** v3.1 frontend refactor (2-day split)
**Scope of this ticket:** the dark monochrome design system, the routing/nav skeleton, the landing hero, and the Forecasts map (3-layer model + forecast card de-dup). After today, the front door (`/`) is done; secondary pages come Day 2.
**Read first:** `docs/v3.1_frontend_plan.md` (full plan), `envision_plan.md` §12 (disclaimer) + §5 (attribution). Backend, agent, Modal, DB, migrations are **untouched** all sprint — this is `viewer/` only.

---

## Pre-flight (verify before editing — the inventory in the plan is from PROGRESS and may have drifted)
1. `cd viewer && git status` clean; `ls viewer/app viewer/components viewer/lib` to confirm the real tree.
2. Confirm these exist: `components/status-header.tsx`, `components/disclaimer-banner.tsx`, `components/layer-visibility-provider.tsx`, `map-layer-panel.tsx`, `signal-layer.tsx`, `polygon-signal-layer.tsx`, `wind-layer.tsx`, `forecast-dropdown.tsx`, `typing-text.tsx`, `app/forecast/[id]/page.tsx`, `lib/layer-state.ts`, `lib/signal-sources.ts`, `app/api/wind/route.ts`.
3. Confirm the exact `signal_type` strings in the DB / `layer-state.ts` so the Detections grouping is correct (esp. `high_wind_corridor`, `cyclone_feature`, `fire_weather_grid`, `heavy_precipitation_band`).
4. Dark basemap is **already applied (your Cursor work)** — do not swap tiles; just verify layers read on it.

## NEVER-CUT (survives every restyle)
- **Disclaimer banner stays on every page** (restyle only). Locked §12 text appears in full on `/disclaimer` (Day 2) + truncated in `<meta name="description">`.
- **Source attribution** is a TOS condition — not deleted anywhere; on the forecast card it collapses to one link per source (Task 5).

---

## Tasks

### 1 — Dark monochrome design system *(do first; everything inherits)*
- Consult the **frontend-design** skill for token discipline before writing styles.
- `globals.css`: invert/remove the v1 `prefers-color-scheme` light-forcing override so the app renders dark by default. Keep `@import "leaflet/dist/leaflet.css"`.
- Tokens: near-black field (~`#0A0A0B`), 1–2 elevated greys, off-white type. **No brand accent — B&W chrome.** Heavy grotesk for display, monospace for labels/data. Generous negative space, big type scale for the hero.
- **Data-viz colors unchanged.** Forecast markers, signal layers, and (Day 2) Brier charts keep their existing palette; only contrast-tune for dark. Color appears *only* inside data viz, never in chrome.
- **Accept:** every page renders dark; no light flashes; type system in place; map layer colors still legible.

### 2 — Routing + nav + footer + header removal
- Create routes: `/evolution` (placeholder ok today — built Day 2), `/disclaimer` (placeholder), `/how-it-works` (placeholder).
- Redirects: `/agent → /evolution`, `/about → /how-it-works` (Next.js `redirects()` in `next.config.js` or route-level).
- **Top nav (3 items):** Forecasts (`/`) · Evolution (`/evolution`) · About (label "About", links `/how-it-works`).
- **Footer:** Disclaimer link → `/disclaimer`. (Disclaimer is footer-only; not in top nav.)
- Remove `<StatusHeader/>` from `app/layout.tsx`. **Keep `LayerVisibilityProvider` and the disclaimer banner.** Do **not** delete the status-query functions — they're reused on How-it-works Day 2.
- Restyle `disclaimer-banner.tsx` to a slim dark strip; keep it globally mounted.
- **Accept:** all routes resolve; both redirects work; nav + footer correct; no status header anywhere; banner present on every page.

### 3 — Landing hero (`/`)
- New `components/hero.tsx` + `components/scroll-arrow.tsx`. Full-viewport section above the existing map on `/`. Layout cloned from the reference, recolored B&W:
  - **Top-left stacked display, ragged-left, oversized:** line 1 grotesk wordmark **ENVISION**; mono line(s) below for descriptor (e.g. `WILDFIRE · CYCLONE` / `GLOBAL · SELF-EVOLVING`). Lots of empty space above.
  - **Lower-left:** uppercase tracked-out mono pitch, ≤2 lines.
  - **Lower-right:** large mono wordmark = **`GLOBAL`** (the "SF.CA" slot).
  - **Background texture:** faint ghosted monospace = **real detection-skill source** (e.g. lines from `agent/modal_skills/wildfire-risk-elevated/run.py`), rendered as static low-opacity text — never executed.
  - **No bloom / glow / gradient.** Black field only; type + ghost-code + negative space carry it.
  - **Scroll cue:** circled down-arrow, center-bottom → smooth-scrolls to the map section. Respect `prefers-reduced-motion` (jump, no smooth).
- **Accept:** hero fills the viewport; map begins exactly one scroll down; arrow scrolls to it; no color anywhere; reduced-motion honored.

### 4 — Forecasts map (3-layer Option C)
- Rename the page concept to **Forecasts** (nav label already set Task 2). Map lives in the section below the hero on `/`.
- Collapse the 9+ toggles to **3** in `lib/layer-state.ts` / `LAYER_TREE`:
  - **Forecasts** — model forecasts (signal-type/forecast set). Default **ON**.
  - **Detections** — all signal_types incl. `high_wind_corridor`, `cyclone_feature`, `fire_weather_grid`, `heavy_precipitation_band`, hotspots, fire alerts, fire-weather, cyclone advisories, heavy-precip. Default **ON**.
  - **Atmosphere** — **gates mounting `<WindLayer/>`** (leaflet-velocity, `/api/wind`); **not** a signal_type filter. Default **OFF**. **Lazy-mount** on enable (it's the heavy layer).
- Rebuild `map-layer-panel.tsx` for 3 items.
- **localStorage migration:** the persisted layer state holds the old 9-layer shape. **Bump the storage key** (or migrate) so stale state can't break the new 3-toggle panel; default to Forecasts+Detections ON, Atmosphere OFF on first load of the new key.
- Forecast + signal layers keep existing colors on the dark basemap; dark-style only the leaflet-velocity legend/displayValues in `wind-layer.tsx`.
- **Accept:** exactly 3 toggles; defaults correct; Atmosphere toggles the velocity field and only mounts when on; `high_wind_corridor` shows under Detections (not Atmosphere); old persisted state doesn't break the panel.

### 5 — Forecast card: signal de-dup + remove skip button
- In `forecast-dropdown.tsx` **and** `app/forecast/[id]/page.tsx`: group contributing signals by **source** (or `source, signal_type`); render **one row per distinct source** with a count (e.g. "NASA FIRMS · 47 detections"). Keep exactly **one attribution link per source** via `signal-sources.ts` (TOS — never zero).
- Keep the LLM reasoning + typing animation; **remove the skip-animation button** from `forecast-dropdown.tsx` / `typing-text.tsx`.
- **Accept:** no repeated NASA-FIRMS (or any) rows; one attribution link per source remains; reasoning still types; no skip button; permalink `/forecast/[id]` matches the dropdown.

---

## Out of scope today
Evolution tree, How-it-works panels + telemetry relocation, Disclaimer content trim, less-wordy copy pass — all Day 2.

## Day-1 done = all of:
- [ ] App renders dark; B&W chrome, data-viz color intact.
- [ ] `/`, `/evolution`, `/disclaimer`, `/how-it-works` resolve; `/agent`→`/evolution`, `/about`→`/how-it-works` redirect.
- [ ] Nav = Forecasts·Evolution·About(`/how-it-works`); footer Disclaimer link; no status header; banner on every page.
- [ ] Hero (no bloom, GLOBAL mark, ghost real-source code, arrow) → one scroll to map.
- [ ] 3 toggles, correct defaults, Atmosphere = lazy WindLayer mount, stale localStorage handled.
- [ ] Forecast card de-duped (one row + one link per source), skip button gone.
- [ ] `npm run build` passes; commit from `~/Downloads/envision/`.
